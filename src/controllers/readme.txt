// contains all the controller web api endpoints and the corresponding controller methods that will be called when the endpoint is hit
// controllers calls the service layer to get the data and returns the data to the client
// service layer calls the repository layer to get the data from the database
// repository layer calls the database to get the data